<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Scrabble" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Scrabble" parent="Base.Theme.Scrabble" />

    <!-- Composant titre par défaut-->
    <style name="TitleTextStyle">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">30sp</item>
        <!--<item name="android:fontFamily">@font/kanit.ttf</item>-->
    </style>

    <!-- Composant texte blanc par défaut-->
    <style name="DefaultWhiteTextStyle">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <!--<item name="android:fontFamily">@font/kanit.ttf</item>-->
    </style>

    <!-- Composant texte noir par défaut-->
    <style name="DefaultBlackTextStyle">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">18sp</item>
        <!--<item name="android:fontFamily">@font/kanit.ttf</item>-->
    </style>

    <!-- Composant texte ok par défaut-->
    <style name="DefaultOKTextStyle">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">21sp</item>
        <!--<item name="android:fontFamily">@font/kanit.ttf</item>-->
    </style>
</resources>